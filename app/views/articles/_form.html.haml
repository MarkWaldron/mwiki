= simple_form_for @article do |f|
  = f.collection_select :category_id, Category.all.order("name ASC"), :id, :name, { prompty: "Choose a Category" }
  = f.input :title
  = f.input :content
  = f.submit
